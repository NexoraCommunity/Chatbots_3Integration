import React from 'react'
import { Input } from './Input'

const SearchBar = () => {
  return (
    <div>
        <Input placeholder="Search..." variant="default" />
    </div>
  )
}

export {SearchBar}